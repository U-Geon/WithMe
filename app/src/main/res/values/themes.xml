<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WithMe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WithMe" parent="Base.Theme.WithMe" />

    <!--  BottomNavigationView 활성화 상태의 메뉴 아이템  -->
    <style name="BottomNavTextActive">
        <item name="android:textSize">12sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:iconTint">@color/black</item>
    </style>

    <!--  BottomNavigationView 비활성화 상태의 메뉴 아이템  -->
    <style name="BottomNavTextInactive">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/inactivate</item>
        <item name="android:iconTint">@color/inactivate</item>
    </style>

    <style name="MyButtonStyle">
        <item name="android:background">@drawable/button_layout</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="ActionBar" parent="Widget.AppCompat.Toolbar.Button.Navigation">
        <item name="android:tint">#ffffff</item>
    </style>
</resources>