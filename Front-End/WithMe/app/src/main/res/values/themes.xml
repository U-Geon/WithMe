<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WithMe" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">#000000</item>
    </style>

    <style name="MyButtonStyle">
        <item name="android:background">@drawable/button_layout</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="UseActionbar" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <item name="android:background">@color/icon_color</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">68dp</item>
    </style>

    <style name="Theme.WithMe" parent="Base.Theme.WithMe" />
</resources>